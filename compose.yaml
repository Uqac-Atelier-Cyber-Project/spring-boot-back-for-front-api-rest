services:
  mysql:
    image: 'mysql:latest'
    environment:
      - 'MYSQL_DATABASE=opibdb'
      - 'MYSQL_PASSWORD=secret'
      - 'MYSQL_ROOT_PASSWORD=verysecret'
      - 'MYSQL_USER=myuser'
    ports:
      - '30037:3306'
